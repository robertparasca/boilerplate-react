import React from 'react';

import LogoImage from '../../assets/images/logo.png';

const Logo = () => {
    return (
        <section id='logo'>
            <img src={LogoImage} />
        </section>
    )
};

export default Logo;
